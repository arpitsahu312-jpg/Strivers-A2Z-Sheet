#include<bits/stdc++.h>
using namespace std;
void explainUSet()
{
    unordered_set<int> st;
    // lower bound and upper bound functions does not work, rest all functions are same
    // as above , it does not stores in any particular order it has a better complexity than set in most cases, except some collision happens
}
int main()
{
    explainUSet();
    return 0;
}